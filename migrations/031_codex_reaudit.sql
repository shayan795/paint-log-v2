-- ============================================================================
-- 031/032: 外部再監査(Codex 2026-08-03)のうち、自分の実装ミス4件 ※本番・開発とも適用済み
-- ============================================================================
-- H-03 停止中でも「同じ大きさ以下」なら画像を上書きできた
--   022でUPDATEにもguardを掛けたが、「大きさが増えないなら素通し」の近道を
--   利用停止(BAN)の判定より手前に置いてしまっていた。
--   停止された人が保存済みのログイン情報で、同じURLのまま画像の中身だけ差し替えられた。
--   → 停止判定を最初に行う。近道してよいのは容量計算だけ。
--
-- H-12 通報された人が退会すると、通報が管理画面から消えていた
--   集計が profiles と内部結合しており、退会でプロフィールが消えると
--   通報記録は残っているのに一覧から落ちた。確認前に退会すれば追跡が切れる。
--   → 外部結合にし、退会済みは「(退会済み)」と表示して一覧に残す。
--
-- M-03 BAN解除しても再登録禁止リストが残っていた
--   誤BANを解除しても、その人が退会→同じメールで再登録すると自動で再びBANされた。
--   → 解除と同じ処理の中で禁止リストからも外す。
--
-- M-05 is_banned(任意UUID) がログイン中なら誰でも呼べた
--   022でbanned_at列を隠した意味が、この関数で消えていた。
--   → 内部用 internal.is_banned(uuid) と、本人専用 public.is_banned()（引数なし）に分離。
--     引数を取らないので他人の状態は原理的に聞けない。
--
-- ★事故の記録（2回目）:
--   public.is_banned(uuid) を先に削除したところ、
--   block_if_banned / block_banned_update / block_banned_profile_update /
--   storage_upload_denied_reason がこれを呼んでいたため、
--   プロフィール更新・投稿・コメントの書き込みが全て失敗する状態になった。
--   読み取りだけを確認して「無事」と判断したのが誤り。すぐ気づいて内部版へ切り替えて復旧。
--   教訓: 関数を消す前に、必ず pg_get_functiondef で全参照元を洗い出し、
--         先に切り替えてから消す。確認は読み取りだけでなく書き込みも通すこと。
