-- ============================================================================
-- 026: 通知の抜け道と、入力の長さ制限【#8 #21 #25】
-- ※ Supabase の SQL Editor で実行してください（025の後）。※本番適用済み(2026-08-01)
-- ----------------------------------------------------------------------------
-- (1) #8 通知の種別を利用者が自己申告でき、'system' と名乗ればレート制限を無視できた。
--        巨大なlinkを付けて連打すればDB無料枠(500MB)を消費でき、
--        上限に達するとサイト全体が書き込み不能になる。
--        → クライアント用RPCは実際に使う2種類だけを受け付け、各項目に長さ制限を付けた。
-- (2) #21 通知の上限に達すると例外を投げ、コメント投稿など元の操作まで巻き添えで失敗していた。
--        → 通知を作らないだけにして、本体の操作は成功させる。
-- (3) #25 画面では制限しているが、RESTを直接叩けば無制限に長い文字列を保存できた。
--        → title/memo/reason/detail などにDB側の長さ制限を追加。
-- （定義は本番へ適用済み。詳細は本番のcatalogを参照）
